{
  "name": "shito-functions",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "lint": "node --check index.js",
    "test": "node --test",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.3",
    "nodemailer": "^8.0.4"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
